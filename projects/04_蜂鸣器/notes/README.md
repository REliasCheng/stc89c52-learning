# 蜂鸣器观察点

[入口代码](../src/course/08-蜂鸣器/src/main.c) 的顺序是：读矩阵键 → 有键时发声 → 更新显示内容 → 刷新数码管。因此如果声音正常但显示闪烁，先量/看 `Int_Buzzer_Buzz()` 占用主循环多久，而不是立即改段码。

`Int_Buzzer_Buzz()` 阻塞主循环约 100 次 1 ms 延时，显示刷新要等它返回。这个时间关系可由源码追出；不同键的实际响度、蜂鸣时是否闪屏没有留存原始记录，不写成确定的板上现象。
