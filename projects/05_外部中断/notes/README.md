# INT0 与消抖

[原入口](../src/course/09-外部中断/src/main.c) 设置 `EA=1`、`EX0=1`、`IT0=1`。下降沿到来后 ISR 延时 10 ms，再检查 `P32 == 0`，最后取反 LED。

这段代码适合看到“轮询与中断的不同”，但中断里等 10 ms 会推迟其他中断处理。等实际接板后，我想先记录一次按键触发次数，再尝试把消抖移到主循环或定时任务；后者必须另开本人实验代码，不覆盖本快照。
