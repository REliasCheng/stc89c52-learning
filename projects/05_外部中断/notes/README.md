# INT0 与消抖

[原入口](../src/course/09-外部中断/src/main.c) 设置 `EA=1`、`EX0=1`、`IT0=1`。下降沿到来后 ISR 延时 10 ms，再检查 `P32 == 0`，最后取反 LED。

这段代码用于对比轮询与中断入口，但 ISR 内等待 10 ms 会推迟其他中断处理。板端验证可记录按键触发次数；非阻塞版本应把消抖移到主循环或定时任务，并放入独立 `practice/` 实现。
