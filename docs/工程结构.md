# 读工程时从哪里下手

[首页](../README.md) · [实验目录](../projects/README.md)

先进入一个主题，再选择 `src/course/` 下的一个完整工程。`src/course/` 表示**课程示例**，不是我独立完成的驱动库。课程前后把模块复制进不同工程是教学过程的一部分；同名文件可能有不同版本，所以没有把它们强行合并。

```text
projects/08_UART/
├─ README.md              这个主题读什么、当前到哪
├─ notes/README.md        代码观察点与待实测项目
└─ src/course/
   ├─ 14-串口-单字节命令/  独立 EIDE 工程
   └─ 15-串口-多字节命令/  另一个独立 EIDE 工程
```

00–02 的 `main.c`、`STARTUP.A51`、`.uvproj` 保留在各工程根目录以维护 Keil 相对路径。后面的 `src/main.c` 是应用流程；`Com/` 放公共类型/延时，`Dri/` 放 Timer0、UART、IIC、1-Wire 等底层，`Int/` 放数码管、按键、EEPROM、OLED 等器件接口。比如 EEPROM 的阅读顺序是 `main → Int_EEPROM → Dri_IIC`，而点阵显示另走 `Int_LEDMatrix → Dri_Timer0`。

原工程各带一份相同的 GPL `stcflash.py`，仓库只留 [`tools/` 的一份](../tools/README.md)；移动工程后 EIDE 配置里的上传脚本相对路径改为 `../../../../../tools/stcflash.py`。这是工程路径调整，不是课程源码逻辑修复。`templates/eide-c51/` 也是课程模板，只作参考。
